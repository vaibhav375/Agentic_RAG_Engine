# Gadgets

A gadget wraps a widget and adds a battery. Create one with
`make_gadget(widget)`.

## Battery

The default battery capacity is 1000 mAh. The capacity can be increased up to a
maximum of 5000 mAh by passing the `capacity` argument.
